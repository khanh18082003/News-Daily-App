import Constants from "expo-constants";

export const API_BASE_URL_BE: string =
  Constants.expoConfig?.extra?.apiBaseUrlBE;

export const API_BASE_URL_PREDICT: string =
  Constants.expoConfig?.extra?.apiBaseUrlPredict;
